using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestPathfind : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Pathfind pathfinding = new Pathfind(10, 10);
    }
}
